<% for photo in @photos%>
    <ul class="clearing-thumbs" data-clearing>
      <li><a href="<%= photo.image%>"><img  width="320" height="240" data-caption="<%= photo.title%>" src="<%= photo.image%>"></a></li>
    </ul>
    <br>
    <hr>
<% end %>